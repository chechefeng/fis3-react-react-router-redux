/**
 * Created by lpw on 2018/3/22.
 */
const initialState = {}
export default function Reducer(state = initialState, action) {
    // console.log(action);
    // console.log(state);USERINFO
    // console.log(actionTypes.MOBILEBOARD_KEY);
    switch (action.type) {
        default:
            return initialState;
    }
}